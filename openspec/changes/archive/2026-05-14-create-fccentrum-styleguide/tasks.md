## 1. Reference Capture & Token Foundation

- [x] 1.1 Capture and document a dated visual reference snapshot of `fccentrum.nl` and `fccentrum.nl/categorie/spots/` for this change.
- [x] 1.2 Extract and define core design tokens (color, typography, spacing, radius, elevation) in an implementation-ready format.
- [x] 1.3 Document approved fallback asset rules (fonts/icons) where exact parity is unavailable.

## 2. Base Styleguide Specification

- [x] 2.1 Document base typography rules for headings, body, links, and metadata text.
- [x] 2.2 Document core component style patterns (buttons/CTA, cards, list items, section headers) including default/hover/focus/active states.
- [x] 2.3 Add responsive behavior guidance for mobile/tablet/desktop (spacing, type scaling, card/list layout transitions).

## 3. Spots UI Theme Application

- [x] 3.1 Map base style tokens to spots-focused UI surfaces (map-adjacent cards/lists, headers, metadata labels, CTA/link treatments).
- [x] 3.2 Define consistent content hierarchy rules for spot entries (title, excerpt, category/meta, actions).
- [x] 3.3 Specify accessibility baseline requirements for spots interactions (focus visibility, contrast baseline, keyboard/pointer parity).

## 4. Quality Gates & Integration Readiness

- [x] 4.1 Create a visual acceptance checklist for brand consistency review prior to demo delivery.
- [x] 4.2 Define deviation logging guidance so non-standard styling decisions are explicitly recorded with rationale.
- [x] 4.3 Validate that styleguide outputs are directly consumable by `render-spots-map` implementation work and update cross-references in change artifacts.